#include "map_parse.h"

std::vector<Trajectory> map_parse::find_trajectory(const Game &game, const Point from, const Point to){
  std::vector<Trajectory> traj;
  return traj;
}

std::vector<Trajectory> map_parse::find_nearest_unwrapped(const Game &game, const Point from, const int max_dist){
  std::vector<Trajectory> traj;
  return traj;
}
