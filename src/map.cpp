#include "map.h"

#include <string>
#include <sstream>
#include <vector>

using Position = std::pair<int, int>;

Map::Map(const std::string& desc) {
  // Parse |desc| and construct a 2D map.
}
